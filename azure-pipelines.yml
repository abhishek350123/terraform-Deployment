# Trigger
trigger:
- main

# Agent / VM
pool:
  vmImage: ubuntu-latest

# Pipeline steps
steps:

- script: echo "Hello World"
  displayName: 'Hello World'

- script: |
    echo "Step 1"
    echo "Step 2"
  displayName: 'Multiple commands'